from <stdlib.krs> import std;

# Prints usage
func printUsage(args: array<str>) {
    std.print("Usage: {args[0]} [options]");
    std.print("version          Display app's version");
    std.print("krabba           Print a nieche krabba");
}

func main(args: array<str>) -> i32 {
    if (args.size < 2) {
        printUsage();
    }

    if (args[1] == "version") {
        std.print("1.0.0");
    }
    elif (args[1] == "krabba") {
        std.print("Krabba!");
    }
    else {
        std.print("Unknown command \"{args[1]}\"");
        return 1;
    }

    return 0;
}